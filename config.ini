; MbClient configuration (INI format)
; This file must sit next to mbclient.bat (i.e. the repo/app root).

[server]
; JS8Call TCP Server API host/port (set JS8Call: File -> Settings -> Reporting -> API)
host = 127.0.0.1
port = 2442

; Message terminator used by the server (leave as-is unless you know why you're changing it)
msg_terminator = ♢


[database]
; db_path sets the location of the Sqlite3 database
; For Windows - db_path = %AppData%/MbClient/
; For Linux - db_path = %HOME%
db_path = %AppData%/MbClient/

[gui]
; The scan_time defines how long the operator must wait between scans
scan_time = 120

; The following settings affect the performance of the GUI.  Don't change unless really necessary.
clock_tick_ms = 20
process_wait_ms = 20


[comms]
; The max_queue_length sets the length of each internal message queue
max_queue_size = 20

; The tx release time increment in seconds
release_time_increment = 15
release_time_increment_short = 5

; The number of seconds a station remains in the station_list before it's aged out
age_out_time = 3600


[debug]
; When true, server can run against simulated messages (developer feature)
debug = false


[logging]
; One of: DEBUG, INFO, WARNING, ERROR, CRITICAL (or a numeric level)
log_level = INFO

; If true, also write logs to a rotating file
log_to_file = true

; Relative paths are relative to the app root
log_file = logs/mbserver.log

; Rotate when the log reaches this size (bytes)
log_max_bytes = 5000000

; Number of rotated log files to keep
log_backup_count = 5
